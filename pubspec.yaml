name: books_rater
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth:
  cloud_firestore:
  firebase_core:
  flutter_riverpod:
  riverpod_annotation:



dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0
  build_runner:
  riverpod_generator:

flutter:

  uses-material-design: true
